Hướng dẫn:

YÊU CẦU ĐÃ CÀI NODEJS NHÉ ANH EM

Chạy lệnh sau để cài các module cần thiết

npm install

Tạo file data.txt ném 12 kí tự vào, mỗi ví là 1 dòng (chưa mở đuôi file thì xóa .txt)
Tạo file proxy.txt ném proxy vào, mỗi proxy là 1 dòng (chưa mở đuôi file thì xóa .txt)

Chạy tool bằng lệnh node + tên tệp

Ví dụ Dapp XL : node XL_monad.js
